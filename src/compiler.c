#include "compiler.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* split a line from the file into opcodes and operands */
char** split_command(char* line) {
  char** ret = malloc(3 * sizeof(char*));
  return ret;
}

int compile(char** commands) {
  return 0;
}
